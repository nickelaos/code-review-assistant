import React from "react";
import { Button } from "antd";

export const SubmitButton = () => {
  return (
    <Button type="primary" className="submit-btn" size="large">
      GET REVIEW
    </Button>
  );
};
